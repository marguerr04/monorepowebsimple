import 'package:flutter/material.dart';

class AppColors {
  // Colores principales basados en tu paleta
  static const Color cyanClaro = Color(0xFF63FFAC);
  static const Color cyanOscuro = Color(0xFF30CBA1); // Este será nuestro primario
  static const Color negro = Color(0xFF000000);
  static const Color blanco = Colors.white;
  static const Color gris = Color(0xFF666666);
  static const Color grisOscuro = Color(0xFF363636);

  // Colores adicionales para UI (puedes ajustarlos)
  static const Color fondoClaro = Color(0xFFF5F5F5); // Un gris muy claro para fondos
  static const Color fondoSidebar = Color(0xFFE0E0E0); // Un gris medio para el sidebar
  static const Color textoClaro = Color(0xFFFAFAFA); // Casi blanco para texto sobre fondos oscuros
  static const Color textoOscuro = Color(0xFF212121); // Gris muy oscuro para texto principal
  static const Color bordeClaro = Color(0xFFBDBDBD); // Gris claro para bordes

  // Colores semánticos (opcional pero útil)
  static const Color exito = Colors.green;
  static const Color error = Colors.red;
  static const Color advertencia = Colors.orange;
}
