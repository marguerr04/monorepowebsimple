import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PaginacuatroPage } from './paginacuatro.page';

describe('PaginacuatroPage', () => {
  let component: PaginacuatroPage;
  let fixture: ComponentFixture<PaginacuatroPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PaginacuatroPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
