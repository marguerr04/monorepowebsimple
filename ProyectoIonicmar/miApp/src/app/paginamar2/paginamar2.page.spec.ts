import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Paginamar2Page } from './paginamar2.page';

describe('Paginamar2Page', () => {
  let component: Paginamar2Page;
  let fixture: ComponentFixture<Paginamar2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Paginamar2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
