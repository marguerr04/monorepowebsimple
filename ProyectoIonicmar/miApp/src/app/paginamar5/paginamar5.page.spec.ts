import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Paginamar5Page } from './paginamar5.page';

describe('Paginamar5Page', () => {
  let component: Paginamar5Page;
  let fixture: ComponentFixture<Paginamar5Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Paginamar5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
