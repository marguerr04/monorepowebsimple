import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Paginamar6Page } from './paginamar6.page';

describe('Paginamar6Page', () => {
  let component: Paginamar6Page;
  let fixture: ComponentFixture<Paginamar6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Paginamar6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
