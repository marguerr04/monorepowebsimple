import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Paginamar3Page } from './paginamar3.page';

describe('Paginamar3Page', () => {
  let component: Paginamar3Page;
  let fixture: ComponentFixture<Paginamar3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Paginamar3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
